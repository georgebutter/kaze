# Kaze UI Component Showcase

To view all components locally, run:

```sh
npm run dev
```

This will start a Vite dev server at [http://localhost:5173](http://localhost:5173) and display the demo page with all available components.

You can add or customize component demos in `demo/App.tsx`.
