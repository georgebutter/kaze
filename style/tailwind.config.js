const {kaze} = require('../src/config');
module.exports = kaze({
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {
    },
  },
  variants: {
    extend: {
    },
  },
  plugins: [],
});

